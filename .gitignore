node_modules/
dist/
build/
*.pyc
__pycache__/
.Python
venv/
.venv-build/
env/
ENV/
*.egg-info/
dist/
build/
.env
.env.local
# Server-specific deployment file (repo name, Watchtower token) — keep local only,
# must not land in the public repo.
docker-compose.prod.yml
.DS_Store
*.log
logs/
*.db
*.sqlite
backend/uploads/*
# Ignore ALL runtime state & secrets under backend/db — notifications.json holds
# the Telegram bot token, printloom.db holds printer access codes. Keep only the
# intentionally tracked seed calibration file.
backend/db/*
!backend/db/macro_configs.json
frontend/.vite
.vscode/
.idea/
*.swp
*.swo
*~
.cache/
.coverage
htmlcov/

# Extrahierte OTTOmat3D-Anleitung (riesig, gehoert nicht ins Repo)
mk01/
*.zip

# Native-Desktop-Build-Artefakte (nicht ins Repo):
#   vendor/ = gebuendelte ffmpeg.exe (~144 MB), Output/ = fertiger Installer.
desktop/vendor/
desktop/Output/
desktop/build/
